apply plugin: 'java'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile 'org.im4java:im4java:1.4.0'
}

repositories {
    mavenCentral()
}

group='com.gla.gradle'   //Group name makes easier to manager the packages.
version='0.1-SNAPSHOT'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../../repo'))
        }
    }
}